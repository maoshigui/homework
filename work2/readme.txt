1.假设有大量的(key,Value)键值对格式数据，希望找出按Value的排序的TOP N的数据，N为可变参数，程序应该把N作为一个变量处理。

假设数据量很大，不能完全放在内存中排序，所以不能直接collect进行排序，也不能使用rdd的top()函数。

 

2.假设有大量的(Key,time,Value)格式数据，求出每个key的移动平均数。比如说我们有性能数据，key为objId+counterId，time为上报时间，value为性能值，

求出每个对象每个计数器的周移动平均数，不足一周的数据按前面所有的数据取平均。
